# 배포 가이드

## 🚀 프로덕션 빌드 및 서빙

### 1. 빌드
```bash
npm run build
```
이 명령어는 `build/` 폴더에 정적 파일들을 생성합니다.

### 2. 로컬에서 서빙 (외부 접근 가능)
```bash
npm run serve
```
- 기본 포트: 3000
- 외부 접근: `http://[컴퓨터IP]:3000`
- 예: `http://172.16.100.132:3000`

### 3. 다른 포트로 서빙
```bash
npm run serve -- --port 8080
```

## 📦 정적 파일로 배포

빌드된 `build/` 폴더의 내용을 다음 플랫폼에 배포할 수 있습니다:

### GitHub Pages
```bash
npm run deploy
```

### Netlify / Vercel
- `build/` 폴더를 업로드하거나
- Git 저장소를 연결하면 자동 배포

### 일반 웹 서버
- `build/` 폴더의 모든 파일을 웹 서버에 업로드
- Apache, Nginx 등에서 정적 파일로 서빙

## 🌐 외부 접근 설정

### 방화벽 설정 (Windows)
1. Windows 보안 → 방화벽 및 네트워크 보호
2. 고급 설정 → 인바운드 규칙 → 새 규칙
3. 포트 → TCP → 3000 → 허용

### 모바일 접속
1. 컴퓨터와 모바일이 같은 Wi-Fi에 연결
2. 컴퓨터 IP 주소 확인: `ipconfig`
3. 모바일 브라우저에서 `http://[컴퓨터IP]:3000` 접속

